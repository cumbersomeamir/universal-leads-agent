# Apify

Scrapers that use [Apify](https://apify.com) actors and API.

## Subfolders

- **Rightmove** – London commercial property to let from [Rightmove](https://www.rightmove.co.uk/commercial-property-to-let/), via Website Content Crawler. Output: `Rightmove/rightmove_london_commercial.xlsx`.

## Usage

Each subfolder has its own `requirements.txt` and run script. Set `APIFY_TOKEN` (or `APIFY_API_TOKEN`) before running.
